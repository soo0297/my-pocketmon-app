// 초기 상태값
const initialState = {
  number: 0,
};

// 리듀서(:변화를 일으키는 함수) 만들기
const pokemon = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default pokemon;
